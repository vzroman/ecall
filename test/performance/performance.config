{role_config, #{
  sender => local,
  receiver => local
}}.

{performance, #{
  pace_ms => 50,
  messages_per_writer => 1000,
  writer_counts => [100000, 200000]
}}.

{env_settings, #{
  ecall_batch_size => 1000,
  distribution_busy_limit_kib => 1024
}}.
