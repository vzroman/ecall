{role_config, #{
  sender => local,
  receiver => local
}}.

{performance, #{
  warmup_ms => 100,
  measurement_ms => 1000,
  drain_ms => 1000,
  report_repetitions => 1,
  smoke_repetitions => 1,
  common_client_counts => [1, 10, 100]
}}.
